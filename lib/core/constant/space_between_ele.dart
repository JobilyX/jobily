import 'package:flutter/material.dart';

import 'spaces.dart';

///Space Between ELements Verticaly
class SpaceV5BE extends StatelessWidget {
  const SpaceV5BE({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: spaceHeightElements5);
  }
}

class SpaceV10BE extends StatelessWidget {
  const SpaceV10BE({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: spaceHeightElements10);
  }
}

class SpaceV15BE extends StatelessWidget {
  const SpaceV15BE({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: spaceHeightElements15);
  }
}

class SpaceV20BE extends StatelessWidget {
  const SpaceV20BE({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: spaceHeightElements20);
  }
}

class SpaceV40BE extends StatelessWidget {
  const SpaceV40BE({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: spaceHeightElements40);
  }
}

class SpaceV80BE extends StatelessWidget {
  const SpaceV80BE({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(height: spaceHeightElements80);
  }
}

class SpaceHBE extends StatelessWidget {
  const SpaceHBE({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(width: spacewidthElements);
  }
}
